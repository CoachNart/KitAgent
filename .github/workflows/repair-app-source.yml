name: Repair App source

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: App.jsx is now maintained directly in main."
