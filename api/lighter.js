const API='https://api.rh.lighter.xyz';
function json(res,status,payload){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify(payload));}
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});try{const path=String(req.query?.path||'/');const r=await fetch(`${API}${path.startsWith('/')?path:`/${path}`}`,{headers:{Accept:'application/json'}});const text=await r.text();let body;try{body=JSON.parse(text)}catch{body={raw:text}}if(!r.ok)return json(res,r.status,{ok:false,error:body?.message||`Lighter returned ${r.status}`,data:body});return json(res,200,{ok:true,data:body,api:API});}catch(error){return json(res,502,{ok:false,error:error?.message||'Lighter request failed'});}}
