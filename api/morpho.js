const API='https://api.morpho.org';
function json(res,status,payload){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify(payload));}
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});try{const route=String(req.query?.route||'markets');const q=new URLSearchParams(req.query||{});q.delete('route');q.set('chainId','4663');let path=route==='vaults'?'/v0/vaults-v2':route==='positions'&&q.get('address')?`/v1/vaults-v2/users/${q.get('address')}/positions`:'/v1/blue/markets';q.delete('address');const r=await fetch(`${API}${path}?${q.toString()}`,{headers:{Accept:'application/json'}});const body=await r.json();if(!r.ok)return json(res,r.status,{ok:false,error:body?.message||`Morpho returned ${r.status}`});return json(res,200,{ok:true,chainId:4663,data:body});}catch(error){return json(res,502,{ok:false,error:error?.message||'Morpho request failed'});}}
