const API='https://api.morpho.org';
function json(res,status,payload){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify(payload));}
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});try{const route=String(req.query?.route||'markets');const q=new URLSearchParams(req.query||{});q.delete('route');let path='/v1/blue/markets';if(route==='vaults'){path='/v0/vaults-v2';q.set('chain_ids','4663')}else if(route==='positions'&&q.get('address')){path=`/v1/vaults-v2/users/${q.get('address')}/positions`;q.set('chain_ids','4663');q.delete('address')}else{q.set('chain_id','4663')}const r=await fetch(`${API}${path}?${q.toString()}`,{headers:{Accept:'application/json'}});const body=await r.json();if(!r.ok)return json(res,r.status,{ok:false,error:body?.message||body?.error?.message||`Morpho returned ${r.status}`});return json(res,200,{ok:true,chainId:4663,data:body});}catch(error){return json(res,502,{ok:false,error:error?.message||'Morpho request failed'});}}
