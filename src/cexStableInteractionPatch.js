(()=>{
const KEY='kitagent_cex_v2';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const n=v=>Number.isFinite(Number(v))?Number(v):0;
const fmt=v=>Number.isFinite(Number(v))?Number(v).toLocaleString(undefined,{maximumFractionDigits:8}):'—';
const arr=v=>Array.isArray(v)?v:(Array.isArray(v?.data)?v.data:[]);
const sym=v=>String(v||'').toUpperCase().replace(/[-/]/g,'').replace('_USDT','USDT').replace('-USDT','USDT');
const css=`#kit-cex-stable{font-family:Inter Tight,Inter,system-ui,sans-serif;background:#070a0f;color:#e7edf7;border:1px solid #171d27;border-radius:12px;overflow:hidden;margin-bottom:10px}#kit-cex-stable *{box-sizing:border-box}#kit-cex-stable button,#kit-cex-stable input,#kit-cex-stable select{font:inherit}#kit-cex-stable .ks-head{display:flex;gap:7px;align-items:center;padding:9px 10px;background:#0a0e14;border-bottom:1px solid #171d27;flex-wrap:wrap}.ks-head select,.ks-head input,.ks-order input,.ks-order select{height:34px;background:#0c1118;color:#eef3f9;border:1px solid #202a38;border-radius:6px;padding:0 9px}.ks-pair{position:relative}.ks-pair>input{width:180px}.ks-menu{position:absolute;z-index:50;top:38px;left:0;width:310px;max-height:360px;overflow:auto;background:#0c1118;border:1px solid #263141;border-radius:8px;box-shadow:0 20px 50px #000;display:none}.ks-menu.open{display:block}.ks-menu input{width:calc(100% - 14px);margin:7px}.ks-item{display:flex;justify-content:space-between;padding:8px 9px;font-size:10px;cursor:pointer}.ks-item:hover{background:#151c27}.ks-item small{color:#657287}.ks-status{font-size:10px;color:#718097}.ks-status.live{color:#25d6d0}.ks-connect,.ks-submit{background:#25d6d0!important;color:#061012!important;border-color:#25d6d0!important;font-weight:800}.ks-connect{margin-left:auto}.ks-market{display:flex;gap:16px;padding:7px 10px;border-bottom:1px solid #171d27;overflow:auto;white-space:nowrap}.ks-metric small{display:block;color:#657287;font-size:8px}.ks-metric b{font-size:11px;font-variant-numeric:tabular-nums}.ks-body{display:grid;grid-template-columns:minmax(0,1fr) 250px 285px;min-height:500px}.ks-chart{padding:10px;border-right:1px solid #171d27;min-width:0}.ks-chart canvas{width:100%;height:390px;display:block;background:#080b10;border-radius:7px}.ks-tfs{display:flex;gap:3px;margin-bottom:7px;overflow:auto}.ks-tfs button,.ks-order button,.ks-tabs button{background:#111721;color:#aab6c7;border:1px solid #263141;border-radius:5px;padding:4px 8px;cursor:pointer;white-space:nowrap}.ks-tfs .active,.ks-order .active,.ks-tabs .active{border-color:#25d6d0;color:#25d6d0;background:rgba(37,214,208,.1)}.ks-book{padding:10px;border-right:1px solid #171d27;overflow:auto}.ks-book h4,.ks-order h4{margin:0 0 9px;font-size:11px}.ks-bookhead,.ks-row{display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px;font-size:9px;padding:3px}.ks-bookhead{color:#657287}.ks-ask{color:#ff5266}.ks-bid{color:#25d6d0}.ks-mid{border-block:1px solid #171d27;margin:4px 0;padding:6px 3px;font-weight:800;font-variant-numeric:tabular-nums}.ks-order{padding:10px;background:#090d13}.ks-sides{display:grid;grid-template-columns:1fr 1fr;gap:4px}.ks-sides button{height:35px}.ks-field{margin:7px 0}.ks-field label{display:flex;justify-content:space-between;color:#718097;font-size:9px;margin-bottom:4px}.ks-field input,.ks-field select{width:100%}.ks-margin{border-top:1px solid #171d27;margin-top:8px;padding-top:8px}.ks-margin input{width:100%;height:34px;background:#0c1118;color:#eef3f9;border:1px solid #202a38;border-radius:6px;padding:0 9px}.ks-size{font-size:9px;color:#657287;margin-top:4px}.ks-quick{display:grid;grid-template-columns:repeat(4,1fr);gap:4px;margin-top:5px}.ks-quick button{font-size:8px}.ks-submit{width:100%;height:38px;margin-top:7px}.ks-account{border-top:1px solid #171d27;padding:8px 10px;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:1px;background:#171d27}.ks-stat{background:#090d13;padding:8px;min-width:0}.ks-stat small{display:block;color:#657287;font-size:8px}.ks-stat b{font-size:10px;white-space:nowrap;font-variant-numeric:tabular-nums}.ks-tabs{display:flex;gap:4px;padding:8px 10px 0}.ks-panel{padding:8px 10px;overflow:auto}.ks-table{width:100%;border-collapse:collapse;font-size:9px}.ks-table th,.ks-table td{text-align:left;padding:6px 5px;border-bottom:1px solid #171d27;white-space:nowrap}.ks-table th{color:#657287;font-weight:500}.ks-error{padding:8px 10px;color:#ff6878;font-size:9px;border-top:1px solid #171d27}.ks-modal{position:fixed;inset:0;z-index:99999;background:#000b;display:grid;place-items:center;padding:16px}.ks-card{width:min(420px,100%);background:#0c1118;border:1px solid #263141;border-radius:12px;padding:18px;box-shadow:0 25px 70px #000}.ks-card h3{margin:0 0 5px}.ks-card p{font-size:9px;color:#718097;margin:0 0 10px}.ks-card input{display:block;width:100%;height:36px;margin:7px 0;background:#080d13;color:#fff;border:1px solid #263141;border-radius:6px;padding:0 9px}.ks-card button{width:100%;height:38px;margin-top:7px;border:0;border-radius:7px;background:#25d6d0;color:#061012;font-weight:800}.ks-card .cancel{background:#151c27;color:#aab6c7}@media(max-width:1000px){.ks-body{grid-template-columns:1fr}.ks-book{border-right:0;border-top:1px solid #171d27}.ks-order{border-top:1px solid #171d27}.ks-chart canvas{height:300px}}@media(max-width:700px){.ks-account{grid-template-columns:repeat(2,minmax(0,1fr))}}`;
if(!document.getElementById('kit-cex-stable-css')){const st=document.createElement('style');st.id='kit-cex-stable-css';st.textContent=css;document.head.appendChild(st)}
const api=(action,state,extra={})=>fetch('/api/cex',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,exchange:state.exchange,symbol:state.symbol,interval:state.interval,key:state.key,secret:state.secret,passphrase:state.passphrase,...extra})}).then(async r=>{const j=await r.json().catch(()=>({}));if(!r.ok||j.error)throw Error(j.error||`Exchange request failed (${r.status})`);return j});
function mount(legacy){if(document.getElementById('kit-cex-stable'))return;const host=legacy?.parentElement||document.querySelector('[data-page="cex"],.cex-page')||document.body;const root=document.createElement('div');root.id='kit-cex-stable';if(legacy)legacy.style.display='none';host.insertBefore(root,legacy||host.firstChild);const saved=(()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}})();const s={exchange:saved.exchange||'mexc',symbol:saved.symbol||'BTCUSDT',interval:'5m',key:saved.key||'',secret:saved.secret||'',passphrase:saved.passphrase||'',connected:false,pairs:[],ticker:null,book:null,candles:[],balance:[],positions:[],orders:[],error:'',side:'buy',orderType:'market',marginMode:'cross',leverage:5,margin:''};
const save=()=>{try{localStorage.setItem(KEY,JSON.stringify({exchange:s.exchange,symbol:s.symbol,key:s.key,secret:s.secret,passphrase:s.passphrase}))}catch{}};
const req=async action=>{try{if(action==='pairs')s.pairs=arr(await api('pairs',s));else if(action==='ticker')s.ticker=await api('ticker',s);else if(action==='book')s.book=await api('book',s);else if(action==='candles')s.candles=toCandles(await api('candles',s));else if(action==='balance')s.balance=arr(await api('balance',s));else if(action==='positions')s.positions=arr(await api('positions',s));else if(action==='orders')s.orders=arr(await api('orders',s));s.error=''}catch(e){s.error=e.message||'CEX request failed'}};
const normalizePairs=()=>{s.pairs=s.pairs.map(p=>({...p,symbol:sym(p.symbol||p.contractId||p.instId||p.name),maxLeverage:n(p.maxLeverage??p.maxLeverageNum??p.leverageMax??p.maxLev??100),contractSize:n(p.contractSize||p.sizeMultiplier||p.contractSizeNum||0),volUnit:n(p.volUnit||p.minVol||0),volScale:Number.isFinite(Number(p.volScale))?Number(p.volScale):undefined})).filter(p=>p.symbol.endsWith('USDT'))};
function toCandles(raw){const d=raw?.data||raw;if(Array.isArray(d))return d.map(x=>Array.isArray(x)?{t:n(x[0]),o:n(x[1]),h:n(x[2]),l:n(x[3]),c:n(x[4])}:{t:n(x.t||x.time)*1000,o:n(x.o||x.open),h:n(x.h||x.high),l:n(x.l||x.low),c:n(x.c||x.close)}).filter(x=>x.o);const t=d?.time||[],o=d?.open||[],h=d?.high||[],l=d?.low||[],c=d?.close||[];return t.map((v,i)=>({t:n(v)*1000,o:n(o[i]),h:n(h[i]),l:n(l[i]),c:n(c[i])})).filter(x=>x.o)}
const pair=()=>s.pairs.find(p=>sym(p.symbol)===sym(s.symbol));
function marginAvailable(){const a=s.balance.find(x=>String(x.currency||x.coin||'').toUpperCase()==='USDT')||s.balance[0]||{};return n(a.availableBalance??a.available??a.equity??a.balance)}
function calcVolume(){const p=pair(),price=n(s.ticker?.lastPrice||s.ticker?.last||s.ticker?.fairPrice);const m=n(s.margin),lev=n(s.leverage),cs=n(p?.contractSize);if(!m||!lev||!price||!cs)return 0;let v=m*lev/(price*cs);if(n(p?.volUnit)>0)v=Math.floor(v/n(p.volUnit))*n(p.volUnit);if(Number.isFinite(p?.volScale))v=Number(v.toFixed(p.volScale));return v}
function draw(){const c=root.querySelector('[data-chart]');if(!c)return;const ctx=c.getContext('2d'),d=s.candles.slice(-120);const w=c.width=c.clientWidth*devicePixelRatio,h=c.height=c.clientHeight*devicePixelRatio;ctx.clearRect(0,0,w,h);if(!d.length)return;const lo=Math.min(...d.map(x=>x.l)),hi=Math.max(...d.map(x=>x.h)),pad=18*devicePixelRatio;const xstep=(w-pad*2)/Math.max(1,d.length-1);d.forEach((x,i)=>{const x0=pad+i*xstep,y=v=>pad+(hi-v)/(hi-lo||1)*(h-pad*2);ctx.strokeStyle=x.c>=x.o?'#25d6d0':'#ff5266';ctx.beginPath();ctx.moveTo(x0,y(x.h));ctx.lineTo(x0,y(x.l));ctx.stroke();ctx.beginPath();ctx.moveTo(x0,y(x.o));ctx.lineTo(x0,y(x.c));ctx.stroke()})}
function render(){const p=pair(),t=s.ticker||{},asks=s.book?.asks||[],bids=s.book?.bids||[],last=n(t.lastPrice||t.last||t.fairPrice);const list=s.pairs.slice(0,200);const av=marginAvailable();root.innerHTML=`<div class="ks-head"><b>Futures</b><select data-exchange><option value="mexc" ${s.exchange==='mexc'?'selected':''}>MEXC</option><option value="bitget" ${s.exchange==='bitget'?'selected':''}>Bitget</option></select><div class="ks-pair"><input data-pair value="${esc(s.symbol)}" autocomplete="off" placeholder="Search pair"><div class="ks-menu"><input data-pair-search placeholder="Search all futures pairs" autocomplete="off"><div data-pair-list>${list.map(x=>`<div class="ks-item" data-symbol="${esc(x.symbol)}"><b>${esc(x.symbol)}</b><small>max ${esc(x.maxLeverage)}x</small></div>`).join('')||'<div style="padding:15px;color:#657287;font-size:10px">No futures pairs loaded</div>'}</div></div></div><span class="ks-status ${s.ticker?'live':''}">${s.ticker?'● Live market data':'○ Loading market data'}</span><button class="ks-connect" data-connect>${s.connected?'Account connected':'Connect exchange'}</button></div><div class="ks-market"><div class="ks-metric"><small>LAST</small><b data-last>${fmt(last)}</b></div><div class="ks-metric"><small>24H</small><b>${t.riseFallRate!=null?(n(t.riseFallRate)*100).toFixed(2)+'%':'—'}</b></div><div class="ks-metric"><small>FAIR</small><b>${fmt(t.fairPrice)}</b></div><div class="ks-metric"><small>INDEX</small><b>${fmt(t.indexPrice)}</b></div><div class="ks-metric"><small>FUNDING</small><b>${t.fundingRate!=null?(n(t.fundingRate)*100).toFixed(4)+'%':'—'}</b></div><div class="ks-metric"><small>MAX LEVERAGE</small><b>${Math.max(1,Math.floor(n(p?.maxLeverage)||100))}x</b></div></div><div class="ks-body"><div class="ks-chart"><div style="display:flex;justify-content:space-between;font-size:11px;margin-bottom:6px"><b>${esc(s.symbol)} Perpetual</b><span class="ks-status ${s.ticker?'live':''}">${s.error?'ERROR':'LIVE'}</span></div><div class="ks-tfs">${['1m','5m','15m','30m','1h','4h','1d'].map(x=>`<button data-tf="${x}" class="${s.interval===x?'active':''}">${x}</button>`).join('')}</div><canvas data-chart></canvas></div><div class="ks-book"><h4>Order Book</h4><div class="ks-bookhead"><span>Price</span><span>Size</span><span>Depth</span></div>${asks.slice(0,14).reverse().map(x=>`<div class="ks-row"><span class="ks-ask">${fmt(x[0]||x.price)}</span><span>${fmt(x[1]||x.size)}</span><span></span></div>`).join('')}<div class="ks-mid">${fmt(last)}</div>${bids.slice(0,14).map(x=>`<div class="ks-row"><span class="ks-bid">${fmt(x[0]||x.price)}</span><span>${fmt(x[1]||x.size)}</span><span></span></div>`).join('')}</div><div class="ks-order"><h4>Order</h4><div class="ks-sides"><button data-side="buy" class="${s.side==='buy'?'active':''}">Open Long</button><button data-side="sell" class="${s.side==='sell'?'active':''}">Open Short</button></div><div class="ks-field"><label>Margin mode</label><select data-margin-mode><option value="cross" ${s.marginMode==='cross'?'selected':''}>Cross</option><option value="isolated" ${s.marginMode==='isolated'?'selected':''}>Isolated</option></select></div><div class="ks-field"><label>Leverage <span data-lev-label>${s.leverage}x</span></label><input data-lev type="number" min="1" max="${Math.max(1,Math.floor(n(p?.maxLeverage)||100))}" step="1" value="${s.leverage}"></div><div class="ks-margin"><label style="display:flex;justify-content:space-between;color:#718097;font-size:9px;margin-bottom:4px">Position margin <span>${av?`Available ${fmt(av)} USDT`:'Connect account for balance'}</span></label><input data-margin type="number" min="0" step="any" value="${esc(s.margin)}" placeholder="Enter USDT margin"><div class="ks-quick">${[25,50,75,100].map(q=>`<button data-q="${q}">${q===100?'MAX':q+'%'}</button>`).join('')}</div><div class="ks-size">Estimated contract size: <b data-size>${fmt(calcVolume())}</b></div></div><div class="ks-field"><label>Order type</label><select data-order-type><option value="market" ${s.orderType==='market'?'selected':''}>Market</option><option value="limit" ${s.orderType==='limit'?'selected':''}>Limit</option></select></div><div class="ks-field" data-price-field style="display:${s.orderType==='limit'?'block':'none'}"><label>Limit price</label><input data-price type="number" step="any" value="${esc(s.price||last||'')}"></div><button class="ks-submit" data-submit>${s.side==='buy'?'Open Long':'Open Short'}</button></div></div><div class="ks-account"><div class="ks-stat"><small>WALLET</small><b>${fmt(s.balance[0]?.balance??s.balance[0]?.equity)} USDT</b></div><div class="ks-stat"><small>AVAILABLE</small><b>${fmt(av)} USDT</b></div><div class="ks-stat"><small>POSITIONS</small><b>${s.positions.length}</b></div><div class="ks-stat"><small>ORDERS</small><b>${s.orders.length}</b></div><div class="ks-stat"><small>PAIR</small><b>${esc(s.symbol)}</b></div></div>${s.error?`<div class="ks-error">${esc(s.error)}</div>`:''}<div class="ks-tabs"><button class="active" data-tab="positions">Positions</button><button data-tab="orders">Open orders</button></div><div class="ks-panel" data-panel><table class="ks-table"><thead><tr><th>Pair</th><th>Side</th><th>Size</th><th>PNL</th></tr></thead><tbody>${s.positions.map(x=>`<tr><td>${esc(sym(x.symbol||s.symbol))}</td><td>${Number(x.positionType??x.posSide)===2?'Short':'Long'}</td><td>${fmt(x.holdVol??x.total??x.size)}</td><td>${fmt(x.unrealised??x.unrealizedPnl??x.profit)}</td></tr>`).join('')||'<tr><td colspan="4">No open positions</td></tr>'}</tbody></table></div>`;
}
async function refresh(full=false){if(full||!s.pairs.length){await req('pairs');normalizePairs()}await Promise.all([req('ticker'),req('book'),req('candles')]);if(s.connected)await Promise.all([req('balance'),req('positions'),req('orders')]);render();draw();}
root.addEventListener('focusin',e=>{if(e.target.matches('[data-pair]')){root.querySelector('.ks-menu')?.classList.add('open');root.querySelector('[data-pair-search]').value=e.target.value||''}});
root.addEventListener('input',e=>{if(e.target.matches('[data-pair-search]')){const q=e.target.value.toUpperCase();root.querySelectorAll('.ks-item').forEach(x=>x.style.display=x.textContent.toUpperCase().includes(q)?'flex':'none');e.stopPropagation()}if(e.target.matches('[data-pair]')){const q=e.target.value.toUpperCase();root.querySelectorAll('.ks-item').forEach(x=>x.style.display=x.textContent.toUpperCase().includes(q)?'flex':'none');e.stopPropagation()}if(e.target.matches('[data-margin]')){s.margin=e.target.value;root.querySelector('[data-size]')&&(root.querySelector('[data-size]').textContent=fmt(calcVolume()))}if(e.target.matches('[data-lev]')){s.leverage=Math.max(1,Math.min(n(e.target.value)||1,Math.max(1,Math.floor(n(pair()?.maxLeverage)||100))));const l=root.querySelector('[data-lev-label]');if(l)l.textContent=s.leverage+'x';root.querySelector('[data-size]')&&(root.querySelector('[data-size]').textContent=fmt(calcVolume()))}});
root.addEventListener('click',async e=>{const item=e.target.closest('.ks-item');if(item){s.symbol=sym(item.dataset.symbol);s.pairs.length&&(save(),await refresh(false));root.querySelector('.ks-menu')?.classList.remove('open');return}const tf=e.target.closest('[data-tf]');if(tf){s.interval=tf.dataset.tf;await refresh(false);return}const side=e.target.closest('[data-side]');if(side){s.side=side.dataset.side;render();return}const q=e.target.closest('[data-q]');if(q){const input=root.querySelector('[data-margin]');if(input){input.value=(marginAvailable()*n(q.dataset.q)/100).toFixed(6);s.margin=input.value;root.querySelector('[data-size]').textContent=fmt(calcVolume())}return}if(e.target.closest('[data-connect]')){showModal();return}if(e.target.closest('[data-submit]')){await submitOrder();return}});
root.addEventListener('change',async e=>{if(e.target.matches('[data-exchange]')){s.exchange=e.target.value;s.symbol='BTCUSDT';s.pairs=[];s.ticker=null;s.book=null;save();await refresh(true)}if(e.target.matches('[data-margin-mode]'))s.marginMode=e.target.value;if(e.target.matches('[data-order-type]')){s.orderType=e.target.value;render() }if(e.target.matches('[data-price]'))s.price=e.target.value});
async function submitOrder(){if(!s.key||!s.secret){showModal();return}const volume=calcVolume();if(!volume){s.error='Enter a position margin; contract size is calculated automatically from the live contract specification.';render();return}const price=n(root.querySelector('[data-price]')?.value||s.ticker?.lastPrice);try{await api('order',s,{side:s.side,orderType:s.orderType,marginMode:s.marginMode,leverage:s.leverage,volume,price});s.error='Order submitted';await refresh(false)}catch(e){s.error=e.message||'Order failed';render()}}
function showModal(){const old=root.querySelector('.ks-modal');if(old)old.remove();const m=document.createElement('div');m.className='ks-modal';m.innerHTML=`<div class="ks-card"><h3>Connect ${s.exchange==='mexc'?'MEXC':'Bitget'} Futures</h3><p>API credentials stay in this browser and are sent only to KitAgent's exchange proxy for signed requests. Never enter a seed phrase or private key.</p><input data-key placeholder="API key" value="${esc(s.key)}"><input data-secret type="password" placeholder="API secret" value="${esc(s.secret)}">${s.exchange==='bitget'?'<input data-passphrase type="password" placeholder="API passphrase" value="'+esc(s.passphrase)+'">':''}<button data-save-connect>Connect</button><button class="cancel" data-close>Cancel</button></div>`;root.appendChild(m);m.addEventListener('click',async e=>{if(e.target.closest('[data-close]'))m.remove();if(e.target.closest('[data-save-connect]')){s.key=m.querySelector('[data-key]').value.trim();s.secret=m.querySelector('[data-secret]').value.trim();s.passphrase=m.querySelector('[data-passphrase]')?.value.trim()||'';try{await api('connect',s);s.connected=true;save();m.remove();await refresh(false)}catch(err){m.querySelector('p').textContent=err.message||'Connection failed'}}})}
refresh(true);setInterval(()=>refresh(false),3000);}
const observer=new MutationObserver(()=>{const legacy=document.getElementById('kit-cex');if(legacy&&!document.getElementById('kit-cex-stable'))mount(legacy)});observer.observe(document.documentElement,{childList:true,subtree:true});const legacy=document.getElementById('kit-cex');if(legacy)mount(legacy);
})();