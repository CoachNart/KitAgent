const LOGO_URL='https://i.postimg.cc/B6bHVQnT/Kitsetsup-Logo-PNG.png';
const num=value=>{const n=Number(String(value??'').replace(/[^0-9.+-]/g,''));return Number.isFinite(n)?n:0};
const esc=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
const readField=(card,label)=>{const rows=[...card.querySelectorAll('.pnl-meta p')];const row=rows.find(x=>x.textContent.trim().toLowerCase().startsWith(label.toLowerCase()));return row?.querySelector('b')?.textContent.trim()||''};
const observeCard=card=>{if(!card||card.dataset.roiFixed==='1')return;card.dataset.roiFixed='1';const strong=card.querySelector('strong');const label=[...card.querySelectorAll('span')].find(x=>x.textContent.trim()==='Unrealized PnL');const pnl=num(strong?.textContent);const margin=num(readField(card,'Margin'));const roi=margin?(pnl/margin*100):0;const positive=pnl>=0;if(strong){strong.textContent=`${positive?'↗':'↘'} ${roi>=0?'+':''}${roi.toFixed(2)}%`;strong.classList.toggle('positive-text',positive);strong.classList.toggle('negative-text',!positive)}if(label)label.textContent='P&L / ROI';const marginRow=[...card.querySelectorAll('.pnl-meta p')].find(x=>x.textContent.trim().toLowerCase().startsWith('margin'));marginRow?.remove()};
const cardData=card=>{const strong=card.querySelector('strong')?.textContent.trim()||'';const pnlPct=strong.replace(/[^0-9.+-]/g,'');return {symbol:card.querySelector('h3')?.textContent.trim()||'Futures Position',roi:pnlPct,entry:readField(card,'Entry'),mark:readField(card,'Mark'),size:readField(card,'Size'),leverage:readField(card,'Leverage'),liquidation:readField(card,'Liquidation'),sl:card.querySelector('.pnl-risk-strip span:first-child b')?.textContent.trim()||'Not set',tp:card.querySelector('.pnl-risk-strip span:last-child b')?.textContent.trim()||'Not set',profile:card.querySelector('.pnl-card-brand b')?.textContent.trim()||'KitSetups Trader'}};
const makeSvg=data=>`<svg xmlns="http://www.w3.org/2000/svg" width="1080" height="1350" viewBox="0 0 1080 1350"><defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop offset="0" stop-color="#070b10"/><stop offset="1" stop-color="#0d1821"/></linearGradient></defs><rect width="1080" height="1350" rx="48" fill="url(#g)"/><rect x="34" y="34" width="1012" height="1282" rx="40" fill="none" stroke="#253340"/><text x="76" y="96" fill="#22c7a5" font-family="Arial" font-size="28" font-weight="800">KITSETUPS</text><text x="76" y="170" fill="#eef3f9" font-family="Arial" font-size="28" font-weight="700">${esc(data.profile)}</text><text x="76" y="204" fill="#718094" font-family="Arial" font-size="18">${esc(data.symbol)}</text><rect x="76" y="250" width="928" height="250" rx="30" fill="#0a1118" stroke="#202d39"/><text x="112" y="300" fill="#718094" font-family="Arial" font-size="17">P&amp;L / ROI</text><text x="112" y="410" fill="${data.roi.startsWith('-')?'#f05b6b':'#22c7a5'}" font-family="Arial" font-size="78" font-weight="800">${esc(data.roi)}%</text>${[['ENTRY',data.entry],['MARK',data.mark],['SIZE',data.size],['LEVERAGE',data.leverage],['LIQUIDATION',data.liquidation],['STOP LOSS',data.sl],['TAKE PROFIT',data.tp]].map((x,i)=>{const col=i%2,row=Math.floor(i/2);const y=550+row*145;return `<text x="${col?596:108}" y="${y}" fill="#718094" font-family="Arial" font-size="16">${x[0]}</text><text x="${col?596:108}" y="${y+40}" fill="#eef3f9" font-family="Arial" font-size="25" font-weight="700">${esc(x[1])}</text>`}).join('')}<text x="76" y="1270" fill="#22c7a5" font-family="Arial" font-size="15" font-weight="700">kitsetups.xyz</text></svg>`;
const pngFile=async(svg,name)=>{const url=URL.createObjectURL(new Blob([svg],{type:'image/svg+xml;charset=utf-8'}));try{const img=new Image();await new Promise((res,rej)=>{img.onload=res;img.onerror=rej;img.src=url});const c=document.createElement('canvas');c.width=1080;c.height=1350;c.getContext('2d').drawImage(img,0,0);const blob=await new Promise(r=>c.toBlob(r,'image/png',1));if(!blob)throw Error('PNG render failed');return new File([blob],name,{type:'image/png'})}finally{URL.revokeObjectURL(url)}};
const handleAction=async(event,kind,card)=>{const button=event.target.closest('.pnl-share-actions button');if(!button||button.classList.contains('ghost'))return;event.preventDefault();event.stopImmediatePropagation();const data=cardData(card);try{const file=await pngFile(makeSvg(data),`kitsetups-pnl-${Date.now()}.png`);if(kind==='share'&&navigator.share&&(!navigator.canShare||navigator.canShare({files:[file]}))){await navigator.share({title:'KitSetups PnL',text:`${data.symbol} · ${data.roi}% ROI`,files:[file]});return}const a=document.createElement('a');a.href=URL.createObjectURL(file);a.download=file.name;document.body.appendChild(a);a.click();a.remove()}catch(error){if(error?.name!=='AbortError')console.error('KitSetups PnL export failed:',error)}};
const scan=()=>{document.querySelectorAll('.pnl-share-card').forEach(observeCard)};new MutationObserver(scan).observe(document.documentElement,{childList:true,subtree:true});scan();document.addEventListener('click',event=>{const button=event.target.closest('.pnl-share-actions button');const card=button?.closest('.pnl-share-card');if(!button||!card||button.classList.contains('ghost'))return;const kind=button.textContent.trim().toLowerCase();handleAction(event,kind,card)},true);
